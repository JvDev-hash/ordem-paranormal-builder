server:
  port: 8546

spring:
  application:
    name: encounters-service
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://db.wtkpqewdyfjkarmwucgj.supabase.co:5432/postgres?user=postgres&password=uGmvzaKyGlvsYfmd

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    open-in-view: false
    generate-ddl: true
